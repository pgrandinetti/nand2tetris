**WARNING**: The compiler in project `10` and `11` doesn't optimize error messages, and assumes that the source code is syntactically correct.
And I tested source code files one-by-one (not walking the folder).
